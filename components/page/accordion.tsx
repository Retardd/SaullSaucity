'use client';
import { useState } from 'react'; import { AnimatePresence,motion } from 'framer-motion'; import { Plus } from 'lucide-react';
export function Accordion({items}:{items:{title?:string;q?:string;text?:string;a?:string}[]}){const [open,setOpen]=useState<number|null>(0);return <div className="mx-auto max-w-3xl space-y-3">{items.map((item,i)=>{const title=item.title||item.q||'';const body=item.text||item.a||'';const active=open===i;return <div className="glass overflow-hidden rounded-3xl" key={title}><button onClick={()=>setOpen(active?null:i)} className="flex w-full items-center justify-between px-6 py-5 text-left"><span className="font-medium">{title}</span><Plus size={18} className={`transition ${active?'rotate-45':''}`}/></button><AnimatePresence>{active&&<motion.div initial={{height:0,opacity:0}} animate={{height:'auto',opacity:1}} exit={{height:0,opacity:0}} className="overflow-hidden"><p className="border-t border-white/10 px-6 py-5 text-sm leading-7 text-white/55">{body}</p></motion.div>}</AnimatePresence></div>})}</div>}
