'use client';
import { motion } from 'framer-motion'; import { cn } from '@/lib/utils';
import Magnet from '@/components/ui/magnet';
export function Glass({children,className,hover=true}:{children:React.ReactNode;className?:string;hover?:boolean}) { return <motion.div whileHover={hover?{y:-5,scale:1.008}:undefined} transition={{type:'spring',stiffness:260,damping:22}} className={cn('glass rounded-[2rem]',className)}>{children}</motion.div> }
export function Button({children,className,href,external=false,onClick}:{children:React.ReactNode;className?:string;href?:string;external?:boolean;onClick?:()=>void}) { const cls=cn('glass inline-flex items-center justify-center gap-2 rounded-full px-5 py-3 text-sm font-semibold transition hover:border-white/35 hover:bg-white/10 active:scale-95',className); const inner = href?<a href={href} onClick={onClick} className={cls} target={external ? '_blank' : undefined} rel={external ? 'noopener noreferrer' : undefined}>{children}</a>:<button onClick={onClick} className={cls}>{children}</button>; return <Magnet padding={80} magnetStrength={18} activeTransition="transform 0.3s ease-out" inactiveTransition="transform 0.5s ease-in-out" wrapperClassName="inline-flex" innerClassName="inline-flex">{inner}</Magnet> }
