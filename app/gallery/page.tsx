import { PageHero } from '@/components/page/page-hero'; import { Glass } from '@/components/ui/glass';
export default function Gallery(){return <><PageHero eyebrow="Fragments of the Society" title="The archive">A future home for moments, artwork, and memories shared by our community.</PageHero><div className="mx-auto grid max-w-6xl grid-cols-2 gap-3 px-5 md:grid-cols-4">{Array.from({length:8},(_,i)=><Glass key={i} className={`relative min-h-40 overflow-hidden p-4 ${i===0||i===5?'md:col-span-2 md:min-h-64':''}`}><div className="absolute inset-0 bg-[radial-gradient(circle_at_70%_20%,rgba(176,129,219,.24),transparent_30%),linear-gradient(140deg,transparent,rgba(31,17,44,.8))]"/><span className="relative eyebrow">Archive · {String(i+1).padStart(2,'0')}</span></Glass>)}</div></>}
